"use client";
import {FormEvent,useEffect,useMemo,useState} from "react";import Link from "next/link";
type H={query:string;resolved:string;kind:string;at:number};type Src={title:string;url:string;excerpt:string;provider:string};type Paper={id:string;query:string;resolved:string;title:string;overview:string;findings:string[];sources:Src[];created:number};
const HISTORY="infinity_phi_context_v1",PAPERS="infinity_phi_research_v1";
const E:Record<string,{symbol:string;number:number}>={hydrogen:{symbol:"H",number:1},helium:{symbol:"He",number:2},boron:{symbol:"B",number:5},carbon:{symbol:"C",number:6},nitrogen:{symbol:"N",number:7},oxygen:{symbol:"O",number:8},fluorine:{symbol:"F",number:9},aluminum:{symbol:"Al",number:13},potassium:{symbol:"K",number:19},iron:{symbol:"Fe",number:26},copper:{symbol:"Cu",number:29},arsenic:{symbol:"As",number:33},selenium:{symbol:"Se",number:34},yttrium:{symbol:"Y",number:39},niobium:{symbol:"Nb",number:41},antimony:{symbol:"Sb",number:51},iodine:{symbol:"I",number:53},dysprosium:{symbol:"Dy",number:66},ytterbium:{symbol:"Yb",number:70},hafnium:{symbol:"Hf",number:72},tantalum:{symbol:"Ta",number:73},tungsten:{symbol:"W",number:74},rhenium:{symbol:"Re",number:75},platinum:{symbol:"Pt",number:78},gold:{symbol:"Au",number:79},mercury:{symbol:"Hg",number:80},lead:{symbol:"Pb",number:82},bismuth:{symbol:"Bi",number:83},uranium:{symbol:"U",number:92}};
const MUSIC=/\b(queen|freddie|music|song|album|singer|band|rock|vocal|concert)\b/i,SCI=/\b(element|atom|atomic|chem|chemistry|metal|oxide|ion|alloy|periodic|material|molecule|electron|isotope|physics|rhenium|helium|yttrium|dysprosium|bismuth|antimony|fluorine)\b/i;
function resolve(q:string,h:H[]){const raw=q.trim(),l=raw.toLowerCase();if(l!=="mercury"){const e=E[l];return e?{kind:"element",resolved:`${raw} chemical element ${e.symbol} atomic number ${e.number}`,element:e}:{kind:"general",resolved:raw};}const c=h.slice(-32).map(x=>`${x.query} ${x.resolved} ${x.kind}`).join(" ");return SCI.test(c)||!MUSIC.test(c)?{kind:"element",resolved:"Mercury chemical element Hg atomic number 80",element:E.mercury}:{kind:"music",resolved:"Mercury music Queen Freddie Mercury"};}
const clean=(s:any)=>String(s||"").replace(/<[^>]+>/g," ").replace(/\s+/g," ").trim();
async function research(q:string):Promise<Src[]>{const out:Src[]=[];const add=(x:Src)=>{if(x.excerpt&&x.title&&!out.some(y=>y.url===x.url))out.push(x)};const wp=fetch(`https://en.wikipedia.org/w/api.php?action=query&generator=search&gsrsearch=${encodeURIComponent(q)}&gsrlimit=12&prop=extracts|info&exintro=1&explaintext=1&inprop=url&format=json&origin=*`).then(r=>r.json()).then(j=>Object.values(j?.query?.pages||{}).forEach((p:any)=>add({title:clean(p.title),url:p.fullurl||"",excerpt:clean(p.extract),provider:"Wikipedia"}))).catch(()=>{});const dd=fetch(`https://api.duckduckgo.com/?q=${encodeURIComponent(q)}&format=json&no_html=1&skip_disambig=0`).then(r=>r.json()).then(j=>{if(j.AbstractText)add({title:clean(j.Heading||q),url:j.AbstractURL||"",excerpt:clean(j.AbstractText),provider:"DuckDuckGo"});(j.RelatedTopics||[]).flatMap((x:any)=>x.Topics||[x]).forEach((x:any)=>x.Text&&add({title:clean(x.Text).split(" - ")[0],url:x.FirstURL||"",excerpt:clean(x.Text),provider:"DuckDuckGo"}))}).catch(()=>{});const cr=fetch(`https://api.crossref.org/works?query=${encodeURIComponent(q)}&rows=12`).then(r=>r.json()).then(j=>(j?.message?.items||[]).forEach((x:any)=>{const title=clean(x.title?.[0]);const abstract=clean(x.abstract);if(title)add({title,url:x.URL||`https://doi.org/${x.DOI||""}`,excerpt:abstract||`${title}. Scholarly work indexed by Crossref${x.publisher?` from ${x.publisher}`:""}.`,provider:"Crossref"})})).catch(()=>{});await Promise.all([wp,dd,cr]);return out;}
function makePaper(query:string,resolved:string,s:Src[]):Paper{const passages=s.map(x=>x.excerpt).filter(Boolean);const overview=passages.slice(0,5).join(" ");const findings=[...new Set(passages.flatMap(x=>x.split(/(?<=[.!?])\s+/)).filter(x=>x.length>55))].slice(0,12);return{id:`phi-${Date.now()}-${Math.random().toString(36).slice(2,8)}`,query,resolved,title:`Understanding ${query}`,overview:overview||`Research sources were reached for ${resolved}, but no usable overview text was returned.`,findings,sources:s,created:Date.now()};}
export default function PhiPage(){const [query,setQuery]=useState(""),[history,setHistory]=useState<H[]>([]),[paper,setPaper]=useState<Paper|null>(null),[busy,setBusy]=useState(false),[err,setErr]=useState("");useEffect(()=>{try{setHistory(JSON.parse(localStorage.getItem(HISTORY)||"[]"));const q=new URLSearchParams(location.search).get("q");if(q)setQuery(q)}catch{}},[]);const recent=useMemo(()=>history.slice(-6).reverse(),[history]);async function submit(e:FormEvent){e.preventDefault();if(!query.trim()||busy)return;setBusy(true);setErr("");const r=resolve(query,history);try{const sources=await research(r.resolved),p=makePaper(query.trim(),r.resolved,sources),next=[...history,{query:query.trim(),resolved:r.resolved,kind:r.kind,at:Date.now()}];setPaper(p);setHistory(next);localStorage.setItem(HISTORY,JSON.stringify(next));const papers=JSON.parse(localStorage.getItem(PAPERS)||"[]");localStorage.setItem(PAPERS,JSON.stringify([...papers,p]));}catch{setErr("Research could not be completed. The subject was preserved; no substitute topic was used.")}finally{setBusy(false)}}return <main style={{minHeight:"100dvh",background:"#06172b",color:"white",fontFamily:"system-ui,-apple-system,sans-serif",padding:"max(22px,env(safe-area-inset-top)) 20px 60px"}}><div style={{maxWidth:820,margin:"auto"}}><header style={{display:"flex",justifyContent:"space-between"}}><Link href="/" style={{color:"white",textDecoration:"none",fontWeight:900,letterSpacing:4}}>INFINITY</Link><span style={{fontSize:12,color:"#8db6d9"}}>PHI / RESEARCH</span></header><section style={{padding:"8vh 0 28px"}}><div style={{width:72,height:72,borderRadius:999,background:"#e3322b",display:"grid",placeItems:"center",font:"36px Georgia",marginBottom:22}}>φ</div><h1 style={{fontSize:"clamp(38px,10vw,68px)",lineHeight:1,margin:"0 0 12px"}}>Research with context.</h1><p style={{color:"#b9cee2",fontSize:18,lineHeight:1.5}}>Phi resolves the subject first, then retrieves from multiple independent sources. The exact resolved subject remains bound to this research package.</p></section><form onSubmit={submit} style={{display:"flex",background:"white",borderRadius:999,padding:7}}><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Try Bismuth, then Mercury…" style={{flex:1,minWidth:0,border:0,outline:0,borderRadius:999,padding:"12px 14px",fontSize:17}}/><button style={{border:0,borderRadius:999,background:"#e3322b",color:"white",fontWeight:900,padding:"12px 18px"}}>{busy?"Researching…":"Research"}</button></form>{err&&<p style={{background:"#5a291f",padding:14,borderRadius:14}}>{err}</p>}{paper&&<article style={{marginTop:30,padding:"clamp(20px,5vw,38px)",borderRadius:26,background:"white",color:"#071526"}}><div style={{fontSize:12,fontWeight:900,color:"#3877a8",letterSpacing:1}}>RESEARCH PACKAGE · {paper.id}</div><h2 style={{fontSize:"clamp(32px,8vw,54px)",lineHeight:1.02,margin:"12px 0"}}>{paper.title}</h2><p style={{color:"#587087",fontWeight:700}}>Resolved subject: {paper.resolved}</p><p style={{fontSize:18,lineHeight:1.7}}>{paper.overview}</p>{paper.findings.length>0&&<><h3 style={{fontSize:24}}>Key findings</h3>{paper.findings.map((x,i)=><p key={i} style={{fontSize:16,lineHeight:1.65}}>{x}</p>)}</>}<div style={{marginTop:26,paddingTop:18,borderTop:"1px solid #dbe4ec"}}><b>{paper.sources.length} research records retained</b><p style={{color:"#587087",lineHeight:1.5}}>Wikipedia, DuckDuckGo and Crossref records are stored with this exact package. The new builder consumes this package ID directly—there is no title matching or latest-topic fallback.</p><Link href={`/phi/build?id=${encodeURIComponent(paper.id)}`} style={{display:"inline-block",marginTop:8,padding:"13px 18px",borderRadius:999,background:"#e3322b",color:"white",textDecoration:"none",fontWeight:900}}>Build this research →</Link></div></article>}{recent.length>0&&<section style={{marginTop:30}}><h2 style={{fontSize:14,letterSpacing:2,color:"#8db6d9"}}>ACTIVE CONTEXT</h2>{recent.map((x,i)=><div key={`${x.at}-${i}`} style={{padding:"10px 0",borderBottom:"1px solid #ffffff1f"}}><b>{x.query}</b><div style={{fontSize:13,color:"#9db7ce"}}>{x.resolved}</div></div>)}</section>}</div></main>}
