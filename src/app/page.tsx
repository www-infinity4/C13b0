// The root URL and /phi/ deliberately render the same current workspace.
// Keeping a separate lightweight landing page made phones appear to switch
// between two different Infinity builds depending on which URL was cached.
export { default } from "@/components/PhiPage";
