// Infinity Phi has one canonical runtime shell. The repository root and /phi
// both render the same search/results experience so there is no competing
// secondary index or cross-system image redirect.
export { default } from "@/components/InfinityImageRouteGuard";
